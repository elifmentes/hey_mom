  `<div class="categories">
  <h1 class="category-title"><%= @category.name %></h1>
  <ul class="category-tasks">
    <% @category.tasks.each do |task| %>
      <li><%= link_to task.title, category_task_path(@category, task) %></li>
    <% end %>
  </ul>
</div>
